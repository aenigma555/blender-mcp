[project]
name = "blender-mcp-server"
version = "0.2.0"
description = "MCP server that bridges an AI assistant to a running Blender instance"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
